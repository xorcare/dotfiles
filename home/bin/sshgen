#!/usr/bin/env bash

A=$1
T=$2

[[ -z "$A" ]] && A='1024'
[[ -z "$T" ]] && T='ed25519'

LANG='en_US.UTF-8'
LANGUAGE='en_US.UTF-8'

comment="`echo "$T" | tr '[a-z]' '[A-Z]'` `date +"%d-%b-%Y %H:%M:%S %Z"` `uname -n`"
echo "${comment}"

ssh-keygen -o -t "$T" -a "$A" -C "$comment"